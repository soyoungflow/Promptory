script: |
  APP_DIR="${{ secrets.EC2_APP_DIR }}"
  APP_DIR="$(echo "$APP_DIR" | xargs)"

  echo "SSH connected"
  echo "Current user:"
  whoami

  echo "App directory:"
  echo "$APP_DIR"

  cd "$APP_DIR"
  pwd
  ls -al
